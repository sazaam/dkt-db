import styled from 'styled-components';

const Wrapper = styled.div`
  height: 25px;
  max-width: fit-content;
  padding: 0 15px;
  background: rgba(157, 22, 57, .008);
  border: 1px solid rgba(157, 22, 57, .5);
  border-radius: 2px;
`;

export default Wrapper;
